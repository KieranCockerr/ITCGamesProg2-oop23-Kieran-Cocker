#include <iostream>

int main()
{
	// Creating a new enum class for inventory items
	enum class InventoryItems {
		NONE,
		HEALTH_PACK,
		POWER_UP,
		SILVER_PACK,
		GOLD_COINS
	};

	InventoryItems invArray[5]; // An array containing the inventory items

	for (InventoryItems value : invArray)
	{
		// Unfinished
	}
}